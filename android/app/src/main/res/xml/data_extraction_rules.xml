<?xml version="1.0" encoding="utf-8"?>
<!--
   Belt-and-suspenders backup and device-transfer exclusions for API 31+ (audit I-13).
   allowBackup is "false"; every app-data domain is excluded from both cloud backup and
   device-to-device transfer so no future file can leak off-device by default.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="file" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="file" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
