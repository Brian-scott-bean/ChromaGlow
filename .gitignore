# Xcode
build/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata/
*.xccheckout
*.moved-aside
DerivedData/
*.hmap
*.ipa
*.xcuserstate
*.xcscmblueprint
.xcode.env.local

# Swift Package Manager
.build/
.swiftpm/
*.resolved

# CocoaPods (not used, but just in case)
Pods/
Podfile.lock

# Carthage (not used, but just in case)
Carthage/Build/

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Xcode Previews
PreviewReplacement*/

# Instruments
*.trace

# Fastlane (future)
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output

# Sensitive / local-only
*.p12
*.mobileprovision
secrets.xcconfig
Config/Secrets.xcconfig

# Local-only Xcode husks at the repo root (an old rename experiment:
# ChromaGlow/ holds only .DS_Store, ChromaGlow.xcodeproj only xcuserdata).
# The live project is HueHome.xcodeproj.
ChromaGlow/
ChromaGlow.xcodeproj/
