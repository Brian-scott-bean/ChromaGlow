---
description: Rules for the Composer feature — composition engine, models, and store.
globs:
  - "**/Composition*.swift"
  - "**/CompositionEngine.swift"
  - "**/CompositionModels.swift"
  - "**/CompositionStore.swift"
---

# Composer Feature Rules

## What's Already Built (DO NOT RECREATE)
- `CompositionModels.swift` — PaletteConfig, MotionConfig, EnvelopeConfig, ReactionConfig, CodableColor, CompositionPreset
- `CompositionEngine.swift` — `CompositionEngine.render()` + `CompositionParamBox`
- `CompositionStore.swift` — JSON persistence + 20 built-in presets
- `StudioViewModel.swift` — `.composition(presetID:)` strategy + `composerStudioCards`
- `UnifiedOrchestrator.swift` — `startCompositionMode()` with DTLS 25fps / REST 5fps

## Key Pattern: Live Slider Updates
Sliders write DIRECTLY to `activeCompositionBox`:
```swift
vm.activeCompositionBox?.palette.saturation = newValue
```
No debounce. No API call. The render loop reads the box each frame (25-50fps).

## Preset Categories
- `.ambient` — Slow, warm, relaxing (Sunset, Ocean, Aurora, Cozy, Heartbeat)
- `.energetic` — Fast, vivid, reactive (Bass Drop, Club Mode, Storm Chase)
- `.holiday` — Seasonal with `seasonMonths` field (Christmas, Halloween, Valentine's, etc.)
- `.myCreations` — User-saved compositions

## Built-in Preset IDs
Built-in presets use deterministic UUIDs (`00000001-...` through `00000003-...`). Never generate random UUIDs for built-ins.
